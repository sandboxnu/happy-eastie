import StatusPage from "../../../components/admin/dashboard/statusPage";

const ResetPasswordError = () => <StatusPage message="The password reset link used is either invalid or it expired. Please try again."/>

export default ResetPasswordError