import StatusPage from "../../../components/admin/dashboard/statusPage";

const SignUpError = () => <StatusPage message="The invite link used is either invalid or it expired. Please ask the administrators for a new invite."/>

export default SignUpError